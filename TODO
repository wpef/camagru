SIGN UP
	-- verify MAIL pattern; --> PATTERN attribute on <input>
	- Primary key = login;
	- Check if mail exists;
	-- Check mot de passs => peut pas etre un mot de la langue francaise

SETUP 
	- Add Sample directory to fill DB


VIDEO 
	-- MERGE cote serveur

UI
	- Verify new name before pushing