SIGN UP
	-- verify MAIL pattern; --> PATTERN attribute on <input>
	- Primary key = login;
	- Check if mail exists;
	- Auto connect after confirm
		- if confirmed-> header(index?log=ok)
		- else header(index);

-- HIDE PASWORD FIELD !!!!!